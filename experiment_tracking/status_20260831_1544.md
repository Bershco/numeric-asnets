# Experiment and cluster status — 2026-08-31 15:44 IDT

Exact live rows are in `live_jobs.csv`.  Completed scores are read from the
experiment-specific ledgers rather than rediscovered from historical logs.

## Cluster workload

| State | Tasks | Requested CPUs | Requested RAM |
|---|---:|---:|---:|
| Running | 64 | 384 | 6,140 GiB |
| Ordinary/dependency pending | 99 | 566 | 5,395 GiB |
| Explicitly user-held | 58 | 340 | 4,130 GiB |

## Live experiments

| Experiment | Running | Pending | Held | Resources currently running | Progress / estimate |
|---|---:|---:|---:|---:|---|
| MAIN-VAL-S2-MCTS | 9 | 4 | 0 | 54 CPUs / 1,080 GiB | Active Drone rows range from minutes to 11.3h; most have reached late instances; 72h cap |
| MCTS-PW-CROSS-DOMAIN | 13 | 0 | 0 | 78 CPUs / 1,560 GiB | 7/20 terminal; Counters rows at 26.4h and FO/Rover at 13--16h; hours to days |
| MCTS-RESOURCE-ROVER | 21 | 0 | 0 | 126 CPUs / 2,520 GiB | Repeated six-hour timeouts dominate; active 16.4--26.0h with 72h caps |
| MPRIME-ANCHOR-CORRECTED-VAL | 1 | 27 | 0 | 6 CPUs / 20 GiB | First full task exposed a wave-evaluator Queue/Pipe compatibility bug; no result is valid yet |
| PRESERVE-3-TERM | 18 | 41 | 0 | 108 CPUs / 864 GiB | Delivery terminal-led Stage 2 active; cell-specific policy controllers installed |
| PRESERVE-TPP-S2 | 2 | 0 | 0 | 12 CPUs / 96 GiB | Epochs 97 and 72; first about 2h from 100, second reaches scheduler limit in about 7.5h |
| MCTS-PW70-CROSS-DOMAIN | 0 | 12 | 0 | 0 | Standard-70 correction waits on memory |
| MCTS-HORIZON-COUNTERS | 0 | 8 | 0 | 0 | Binding-horizon pilot waits on memory |
| MCTS-DETERMINISM-AUDIT | 0 | 6 | 0 | 0 | Six smoke-tested one-instance jobs wait on memory |

## Explicit Slurm holds

| Experiment | Held tasks | Requested CPUs | Requested RAM | Reason |
|---|---:|---:|---:|---|
| MPrime validation-led Stage 2 | 17 | 98 | 769 GiB | Invalid anchor plus controller |
| MPrime terminal-led Stage 2 | 21 | 122 | 961 GiB | Reuses corrected validation-led anchor |
| FO Counters legacy MCTS | 20 | 120 | 2,400 GiB | Deliberately deferred |

## MPrime corrective-rescore defect

Preflight `20768884` completed but was a false pass.  The legacy wave evaluator
passed a multiprocessing Queue to a worker entry point changed by the lifecycle
refactor to call `send()` on a Pipe.  Every worker crashed with
`AttributeError: 'Queue' object has no attribute 'send'`, while the wrapper
still wrote a `.done` marker.  Full-array task `20768902_0` reproduced the
failure; the other 27 tasks remain memory-pending.

Local repair:

- one compatibility sender supports both Pipe `send()` and Queue `put()`;
- a regression test covers both contracts;
- the rescore wrapper refuses worker-crash logs and requires a nonempty summary
  before writing `.done`.

No MPrime anchor score from these jobs is scientifically valid yet.

## Reporting contract

Every MCTS comparison must contain 30-minute, 2-hour and 6-hour conservative
coverage.  Policy-only comparisons explicitly say that the cutoff is not
applicable.

## Immediate held-work priority

After the mandatory MPrime correction, `MCTS-PW-30M` is the highest-priority
held experiment.  It directly tests the advisor-relevant 30-minute budget,
uses existing tested search code, is much cheaper than fresh training, and is
already supported by positive PW screening cells.  `STOP-ORIG` is the next
mainstream-methodology priority but is substantially more expensive.  The
action-history ablation remains held behind both.
