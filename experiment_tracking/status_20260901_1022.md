# Experiment status — 1 September 2026, 10:22 IDT

This is the authoritative live status for the snapshot. Scheduler resources
are in `live_snapshot_20260901_1022.csv`; cross-domain PW comparisons are in
`mcts_progressive_widening_cross_domain/comparative_summary_20260901_1022.csv`.

## Scheduler

| State | Jobs | CPU | RAM |
|---|---:|---:|---:|
| Running | 67 | 418 | 4,760 GiB |
| Ordinary pending | 0 | 0 | 0 GiB |
| Dependency pending | 5 | 10 | 5 GiB |
| Operationally held/requeued | 1 | 10 | 20 GiB |
| Deliberately held | 58 | 340 | 4,130 GiB |
| Completing | 0 | 0 | 0 GiB |

The four jobs previously observed in `COMPLETING` were ordinary short
Zenotravel policy evaluations caught during Slurm cleanup. They have left the
queue; no completing job is stuck.

## Live training and policy

`PRESERVE-3-TERM` has 20/60 terminal training lineages and 40 running:
Delivery off 4/10, Delivery on 2/10, TPP off/on 0/10, Zenotravel off 10/10,
and Zenotravel on 4/10. The 40 trainers request 240 CPUs and 1,920 GiB. Five
cell-specific controllers remain dependency-gated, so a VH cell releases its
own policy curve without waiting for the opposite VH mode. Zenotravel/off has
204/209 policy jobs off the live queue, four running, and one requeued/held
after an environment-retrieval failure.

The older validation-led TPP tail was not checkpoint-free. Timed-out job
`20684890` saved snapshots through epoch 79 and printed epoch-80 validation,
but the controller only accepted a terminal `Last valid checkpoint is ...`
log marker. Scheduler termination prevented that marker, so controller
`20743985` skipped the lineage instead of selecting snapshot 79. This is a
controller fallback limitation, not evidence that the training produced no
checkpoint. The other lineage's 22 missing policy rows completed. A proper
fallback must resolve the logged snapshot directory and verify the highest
complete snapshot before materializing this sixteenth held-out lineage.

## MPrime coefficient correction

The corrected rescore is terminal at 540/588 points. Three native child
processes ended with return code `-4`, meaning signal 4 (`SIGILL`), leaving
exactly 48 resumable points across three lineages. This is not the old
Queue/`send()` defect. The 36 MPrime Stage-2 training jobs and two policy
controllers remain deliberately held; no coefficient is frozen from the
incomplete curves.

## Live MCTS

- `MAIN-VAL-S2-MCTS`: 19/20 Drone rows are terminal. One VH-on row runs; it
  has classified 18 instances but has not yet printed a success. The complete
  nine-seed VH-on subset remains policy 5.11 versus MCTS 11.11/11.44/11.44 at
  30m/2h/6h, paired change +6.33 [4.23, 8.44], raw p=.0039.
- `MCTS-PW70-CROSS-DOMAIN`: all 12 standard-budget correction jobs run. None
  is terminal, so every current score is a lower bound. The mixed-budget
  predecessor is complete and VAL-confirmed; FO Counters/Rover used PW70,
  while Block Grouping/Counters used PW20.
- `MCTS-HORIZON-COUNTERS`: all eight jobs run. Current aware/unaware lower
  bounds are close, and every parsed depth summary still has zero horizon
  cutoffs. This is not yet evidence for or against the correction.
- `MCTS-LEGACY-ROVER`: two rows remain active. Job `20559583` ended OOM after
  a retry; its final line says 5/20, while ten plans were printed across
  attempts and the strict validator rejected the mismatch. It is excluded
  from the aggregate until deduplicating post-hoc VAL establishes the unique
  result. The clean rows still show off 3.8 policy to 4.2 MCTS (n=10,
  p=.125) and on 4.0 to 4.57 (n=7, p=.125): positive observed changes, not
  statistically significant claims.

## Completed diagnostics that govern interpretation

`MCTS-DETERMINISM-AUDIT` is complete. Same-node-family repeats were exact;
different CPU families reproducibly changed policy/statistics checksums but
changed no selected action or outcome. The second audit ran only the unaware
arm and changed the worker/instrumentation path, so it did not directly replay
the historical aware/unaware pair. The historical three-instance Horizon
difference therefore remains unexplained. A causal replay requires matched
aware/unaware arms on the same current commit and node/worker configuration.

`MCTS-SAFE-CONTEXT` remains a completed negative result and must not replace
physical-state nodes. `MCTS-SAFE`, Drone Horizon, Drone PW, PW sensitivity,
Counters width sensitivity, MAIN-VAL/MAIN-TERM policy, MPrime validation
redesign and Zenotravel validation-led preservation remain completed/static.

## Held registry

Slurm-held: 38 MPrime Stage-2 jobs/controllers and 20 FO Counters MCTS jobs.
Design-held only: `MCTS-PW-30M`, `ACT-HISTORY-ABLATION`, `MCTS-SAFE2`,
`MCTS-PW-PATHBATCH`, `STOP-ORIG`, `PUCT-EST`, lifecycle-safe two-worker/160-GiB
resource resumptions, and Long-Drone selected-checkpoint MCTS endpoints.
