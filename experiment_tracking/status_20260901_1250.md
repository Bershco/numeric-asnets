# Corrective-action status — 1 September 2026, 12:50 IDT

The last verified scheduler snapshot remains 10:37:46 IDT. Three correctly
spaced attempts through the documented normal Windows profile and
`uni-cluster` alias failed to establish a remote session. The Check Point client
reported `connected` with approximately nine hours remaining, but the SSH route
was not usable. No scheduler state is inferred after 10:37.

Local implementation is complete for every user-approved correction:

- terminal training snapshot-directory fallback for the sixteenth TPP lineage;
- retry-aware plan deduplication and post-hoc VAL for Rover job 20559583;
- a three-index/48-point resumable MPrime corrected-validation continuation;
- a correct same-node, three-worker, aware-versus-unaware determinism audit;
- mandatory cluster preflight for the corrected determinism audit.

Python compilation and five focused recovery tests pass. Git-Bash syntax checks
pass for every new Slurm/submission script. Commit `25a161a3` contains the code.
Exact dispatch scopes and resource requests are frozen in
`approved_recovery_actions_20260901.csv`. None was partially submitted while
the SSH route was unavailable.

The current PW recommendation is to expand FO Counters first from two to five
matched PW70 seeds (six new jobs across both VH modes), with Rover as the second
confirmation priority. Block Grouping and Counters should wait for their active
PW70 correction. No expansion was submitted because the user explicitly asked
for the recommendation before approval.

## Suspected cluster-wide outage preparation

Reports of a university power outage mean the next successful SSH connection
must be treated as disaster recovery, not as an ordinary queue refresh. The
10:37 scheduler state is now frozen per job in
`pre_outage_job_inventory_20260901_1037.csv`. The read-only
`scripts/post_outage_cluster_audit.py` joins current `squeue`, `sacct`, bounded
log evidence, checkpoint directories, and evaluation completion records; it
computes remaining allocation time and recommends—but never performs—the
appropriate continuation. The reviewed sequence and classification rules are
in `cluster_outage_recovery_plan.md`.
