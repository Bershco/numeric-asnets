# Experiment and cluster status — 31 August 2026 20:35 IDT

This is a fresh scheduler-verified snapshot from the documented
`C:\Users\roeeh` / `uni-cluster` SSH profile. Held tasks are separated from
ordinary pending tasks even though Slurm represents both as pending states.

## Workload

| State | Tasks | CPUs | RAM GiB |
|---|---:|---:|---:|
| Running | 76 | 456 | 6,128 |
| Ordinary/dependency pending | 82 | 440 | 4,279 |
| User-held | 58 | 340 | 4,130 |

## Live and pending experiments

| Experiment | Running | Pending | CPU/RAM currently running | Current position | Timing |
|---|---:|---:|---:|---|---|
| MAIN-VAL-S2-MCTS | 7 | 1 | 42 CPU / 840 GiB | 8/16 gap jobs terminal; four historical rows retained | running jobs 3.2--9.6h; 72h cap |
| MCTS-PW-CROSS-DOMAIN | 9 | 0 | 54 CPU / 1,080 GiB | 11/20 terminal | remaining jobs 18--31h; 72h cap |
| MCTS-PW70-CROSS-DOMAIN | 0 | 12 | 0 | 0/12 terminal | start unknown; memory-pending |
| MCTS-HORIZON-COUNTERS | 0 | 8 | 0 | 0/8 terminal | start unknown; memory-pending |
| MCTS-RESOURCE-ROVER | 20 | 0 | 120 CPU / 2,400 GiB | terminal-led jobs remain; extra validation-led job is terminal | active 23--31h; timeout-heavy; 72h cap |
| MPRIME-ANCHOR-CORRECTED-VAL | 4 | 24 | 24 CPU / 80 GiB | at least 19 new checkpoint summaries visible from the first four tasks | 24h/task cap; start of remaining tasks depends on memory |
| PRESERVE-3-TERM | 35 | 24 | 210 CPU / 1,680 GiB | 1/60 terminal | staggered; Delivery up to ~20h, TPP newly started, Zenotravel pending; 72h cap |
| PRESERVE-TPP-S2 | 1 | 0 | 6 CPU / 48 GiB | 15/16 held-out lineages terminal | job 20684890 reaches its 72h limit at 23:26:41 IDT |
| Policy dependency controllers | 0 | 7 | 0 | six PRESERVE-3 cell controllers plus final validation-led TPP controller | normally minutes after their dependencies finish |
| MCTS-DETERMINISM-FOLLOWUP | 0 | 6 | 0 | preflight passed in 1m29s; six node-pinned jobs eligible | node availability controls start; each prior target run took ~7m, 2h cap |

## Newly terminal evidence

Counters validation-led Stage2/on seed `2011206605`, PW20 job `20726021`:

| Policy | Fixed narrow 5/20: 30m / 2h / 6h | PW20: 30m / 2h / 6h | Outcome |
|---:|---:|---:|---|
| 7/59 | 14 / 14 / 14 | 12 / 12 / 12 | 59/59 classified; 12/12 plans VAL-valid; 20h30m |

PW20 improves over policy but trails fixed narrow by two. Its successful plans
are all found within 482.39 seconds. The long whole-job runtime is caused by 47
unsuccessful 10,000-action trajectories, not by successful plans appearing
after two hours. The corresponding standard-budget PW70 job is `20755801` and
is still memory-pending.

The second Counters Stage2/off PW20 seed, job `20726017`, remains running. It
currently has 40 successes; all 40 are within 30 minutes. Its first 40
instances succeeded, later instances either exhausted 10,000 actions or hit
the six-hour timeout. It is processing the final three instances, so a result
is plausible within about six hours, subject to wrapper/VAL cleanup.

Rover validation-led Stage2/off seed `1972442430`, job `20559495`:

| Policy | MCTS 30m / 2h / 6h | Slurm outcome | Evidence |
|---:|---:|---|---|
| 4/20 | 4 / 4 / 4 | OOM after output | all 20 classified; four printed plans; 4/4 VAL-valid |

This is a valid conservative fixed-budget result despite the scheduler state.
The OOM is retained as an outcome column rather than silently converting it to
a normal completion.

## MAIN-VAL Stage2 Drone MCTS

Every terminal addition is VAL-valid. Cutoff columns are deterministic
post-hoc per-instance limits over recorded runtimes.

| VH | Seed | Policy | MCTS 30m | MCTS 2h | MCTS 6h |
|---|---:|---:|---:|---:|---:|
| off | 534933607 | 8 | 10 | 11 | 11 |
| off | 923500475 | 3 | 3 | 3 | 3 |
| off | 1239739722 | 4 | 6 | 6 | 6 |
| off | 1510771779 | 3 | 5 | 5 | 5 |
| off | 1972442430 | 6 | 8 | 8 | 8 |
| off | 2011206605 | 4 | 6 | 6 | 6 |
| off | 2082152039 | 6 | 8 | 8 | 8 |
| on | 923500475 | 6 | 14 | 14 | 14 |

These eight new rows plus four historical rows give 12/20 available MCTS
endpoints. Seven jobs run and one is memory-pending. Final ten-seed-per-VH
confidence intervals and paired significance require those remaining eight
MCTS outcomes, not additional policy evaluations.

## PW20 versus PW70

The mixed screen contains deliberately different budgets:

- Block Grouping and Counters rows are PW20, matched to fixed narrow 5/20.
- FO Counters and Rover rows are PW70, matched to fixed normal 20/70.
- twelve separate Block Grouping/Counters PW70 corrections are pending.

All four currently terminal intended-PW70 cross-domain rows have identical
30-minute, two-hour, and six-hour successful-coverage counts. Thus no recorded
success in that subset requires more than 30 minutes. Their long allocation
times come from failed/timeout/OOM tails. This supports a fresh hard-30-minute
confirmation, but it remains screening evidence until both seeds and both VH
cells finish.

## Corrected MPrime validation

The corrected array `20785172_[0-27]` has four tasks running and twenty-four
ordinary memory-pending. The first four logs already contain at least nineteen
completed checkpoint summaries; no per-VH coefficient can yet be frozen.

The 29/30 preflight was intentionally only a harness check: one anchor-zero,
epoch-zero checkpoint over the frozen thirty-instance validation set. It proved
that inference, worker IPC, completion markers, and VAL worked. The high score
is an early saturation warning, not coefficient evidence. All 588
checkpoint/coefficient points must be compared. If both VH modes again choose
zero only through ties, MPrime Stage2 stays held and the validation curves are
audited. A zero coefficient is accepted only if it wins by a real AUC/peak/final
margin.

Held MPrime counts are not missing seeds:

- validation-led: 16 held-out training jobs + 1 controller = 17; the four
  tuning-winning lineages are reused to give twenty scientific lineages;
- terminal-led: 20 distinct final-checkpoint training jobs + 1 controller =
  21; validation-led tuning lineages cannot be reused because the source
  checkpoints differ.

## Policy follow-ups

Seven lightweight dependency jobs are present:

- Delivery off/on terminal-led cell controllers: two;
- TPP off/on terminal-led cell controllers: two;
- Zenotravel off/on terminal-led cell controllers: two;
- final validation-led TPP controller `20743985`: one.

Each controller releases every-five learning-curve evaluations plus selected
and final endpoints only when its own domain/VH cell is complete. The TPP
controller already exists and should not be duplicated. The one terminal
PRESERVE-3 lineage is Delivery/off seed `2082152039` (job `20755729`, completed
in 14h14m); its cell controller correctly waits for the other nine off lineages.

## Determinism follow-up

The static join of zero-cutoff Horizon jobs `20684991` and `20684992` is done.
The aware run lost `problem_3_3_4`, `problem_4_2_5`, and `problem_8_1_4` and
gained none. `problem_3_3_4` is the cheapest diagnostic target: unaware solved
in 404.85 seconds; aware ended unsolved in 379.24 seconds. The bounded follow-up
uses one worker, two CPUs, 20 GiB, a two-hour cap, deterministic CPU settings,
three repeats pinned to `cs-cpu-07`, and three pinned to `ise-cpu-intl-07`.

Preflight job `20788665` completed successfully in 1m29s. The six diagnostic
jobs `20788666`--`20788671` are now ordinary-pending and eligible. They are
node-pinned by design, so their start depends on the two named nodes rather
than only on the general memory pool.

The previous finding remains: same-node repeats were identical, while moving
node family changed network-policy checksums at 78/105 decisions and child
statistics at 45/105 without changing the selected action on the original
test instance. This localizes numerical variation to CPU/node-dependent
floating-point execution, not process timing. The follow-up asks whether that
variation reaches the selected action on an outcome-divergent instance.

## Held work

| Experiment | Held jobs | Why |
|---|---:|---|
| MPrime validation-led Stage2 | 17 | corrected coefficient not frozen |
| MPrime terminal-led Stage2 | 21 | must reuse corrected per-VH coefficient |
| Legacy FO Counters MCTS | 20 | obsolete 3-worker/120-GiB form; later use lifecycle-safe 2-worker/160-GiB continuation |
| LONG-DRONE selected-checkpoint MCTS | 3 registry-held | policy and final-checkpoint endpoints are complete; selected-checkpoint MCTS would finish the selected-versus-final side comparison |
| ACT-HISTORY-ABLATION | design only | requires fresh Stage1 networks on Drone, Counters, and TPP; deliberately held by user |
| MCTS-PW-30M | design ready | wait for cross-domain screen to select confirmatory cells |
| MCTS-SAFE2 | design only | horizon-indexed nodes risk multiplicative memory; deliberately held |
| STOP-ORIG | design ready | separate original training-success stopping replication; wait for primary workload room |
| MCTS-PW-PATHBATCH | design only | nonstandard multiple widening expansions per selected path; requires implementation/tests |
| PUCT-EST | deliberately held | causal PUCT/estimator sensitivity follows primary RQ evidence |

Zenotravel validation-led Stage2 remains a completed preservation result and is
not live: VH-off stays 20.0/20; VH-on changes 20.0 selected to 19.9 Stage2
selected. Its terminal-led counterpart is part of PRESERVE-3-TERM and is still
memory-pending.
