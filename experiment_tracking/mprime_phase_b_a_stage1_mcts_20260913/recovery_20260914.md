# Exact MPrime Stage-1 fixed-MCTS recovery

All twenty original fixed-search jobs are terminal. Parsing every immutable
attempt log—not merely the success-only completion JSONL—found 192/200 VH-off
and 193/200 VH-on instance outcomes already classified as success, ordinary
unsolved, or six-hour timeout. The remaining fifteen identities were aborted
by allocation OOM before any terminal instance record.

`recovery_manifest_20260914.csv` freezes only those fifteen exact instances.
Each recovery task retains the original checkpoint and normal 20-child,
70-simulation, PUCT 0.1, estimator 0.5 configuration. It uses one worker,
2 CPUs, 120 GiB, a six-hour per-instance limit, and a seven-hour allocation.
One worker avoids reproducing the original three-worker memory collision; the
search performed for an individual instance is unchanged. Previously classified
timeouts are not rerun.

Results remain lower bounds until all fifteen exact tasks terminate. Each task
writes its own attempt log, completion JSONL and VAL summary under
`/home/hersco/training_new_domains/2026-09-14/mprime_phase_b_a_stage1_mcts_recovery`.

Final scientific array `21253780[0-14]` is submitted; task 0 entered the exact
instance-10 evaluator on `ise-cpu256-04`, and the other tasks may remain
resource-pending. Only confirmed native-incompatible nodes `ise-cpu-intl-01`
and `ise-cpu-intl-13` are excluded. Pre-inference deployment attempts are
retained in `recovery_submissions_20260914.tsv` and contribute no results.

At 12:24 IDT, twelve tasks were running. Tasks 7--9 failed before inference
with native exit `-4` on `ise-cpu-intl-24`, `ise-cpu-intl-25`, and
`ise-cpu-intl-11`; they require exact three-task replacement. These failures
do not change a score and must not be counted as classified outcomes.
