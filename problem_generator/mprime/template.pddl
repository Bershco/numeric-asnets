(define (problem ${instance_name})
   (:domain mystery-prime-typed)
   (:objects ${foods} - food
             ${pleasures} - pleasure
             ${pains} - pain
)
   (:init
${initial_statements})
   (:goal (and ${goal_conditions})))
